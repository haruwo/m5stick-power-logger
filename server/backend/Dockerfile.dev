FROM node:18-alpine

WORKDIR /app

RUN apk add --no-cache curl

COPY package*.json ./

RUN npm install

COPY src/ ./src/

RUN mkdir -p logs && chown -R node:node logs

USER node

EXPOSE 3000 9229

# Enable Node.js debugging
CMD ["npm", "run", "dev"]